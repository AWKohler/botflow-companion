# Botflow Companion engine dependencies.
#
# NOTE: these are imported LAZILY (inside auth/signing functions), so the engine
# can start and serve /health + /devices without them. They're required for
# Apple-ID auth and IPA signing.
requests>=2.31
cryptography>=42
srp>=1.0.21
# Anisette generation on non-Apple hosts (unicorn-based ADI emulation) + IPA
# inspection during signing.
unicorn>=2.1
lief>=0.16
Anisette>=1.2
# Windows tray app (app.py). The UI is the engine-served HTML opened in the
# default browser — no embedded webview (pywebview/pythonnet fast-fails when
# frozen with PyInstaller).
pystray>=0.19
pillow>=10.0
# Developer Mode enable + (future) richer device ops; pure-Python over usbmux.
pymobiledevice3>=4.0
# pymobiledevice3 needs win32security on Windows.
pywin32>=306; sys_platform == "win32"
